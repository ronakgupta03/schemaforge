FROM python:3.12-slim
WORKDIR /srv
COPY core/ ./core/
COPY agent/ ./agent/
COPY skills/ ./skills/
RUN pip install --no-cache-dir -e ./core
ENV SF_REGISTRY_HOST=0.0.0.0 SF_REGISTRY_PORT=9010 TRUEFORGE_URL=http://trueforge.internal SF_AGENT_DIR=/srv/agent
EXPOSE 9010
CMD ["sf-registry"]
